/**
 * Represents a single event data structure
 */
export interface EventData {
  image?: string;
  image_alt?: string;
  title?: string;
  link?: string;
  description?: string;
  category?: string;
  when?: string;
  where?: string;
  location_link?: string;
}
